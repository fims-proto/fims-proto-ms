version: '3.4'

services:
  fimsprotoms:
    image: fimsprotoms
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8080:8080
    environment:
      - GIN_MODE=release