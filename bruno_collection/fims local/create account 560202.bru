meta {
  name: create account 560202
  type: http
  seq: 16
}

post {
  url: {{base_path}}/sob/{{sob}}/accounts
  body: json
  auth: inherit
}

body:json {
  {
    "balanceDirection": "debit",
    "class": "5",
    "group": "503",
    "levelNumber": 2,
    "superiorRawAccountNumber": "005602",
    "title": "其他"
  }
}

tests {
  test("Create account", function () {
    expect(res.getStatus()).to.equal(201)
    expect(res.body.rawAccountNumber).to.equal('005602000002')
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
