meta {
  name: cancel audit
  type: http
  seq: 16
}

post {
  url: {{base_path}}/sob/{{sob}}/journal/{{current_journal_id}}/cancel-audit
  body: json
  auth: none
}

body:json {
  {
      "auditor": "{{user_3}}"
  }
}

tests {
  test("Cancel audit journal", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
