meta {
  name: create account 560203
  type: http
  seq: 18
}

post {
  url: {{base_path}}/sob/{{sob}}/accounts
  body: json
  auth: inherit
}

body:json {
  {
    "balanceDirection": "debit",
    "class": "5",
    "group": "503",
    "levelNumber": 3,
    "superiorAccountNumber": "5602",
    "title": "工资"
  }
}

tests {
  test("Create account", function () {
    expect(res.getStatus()).to.equal(201)
    expect(res.body.accountNumber).to.equal('560203')
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
