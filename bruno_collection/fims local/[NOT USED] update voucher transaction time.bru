meta {
  name: [NOT USED] update voucher transaction time
  type: http
  seq: 27
}

patch {
  url: {{base_path}}/sob/{{sob_1}}/voucher/{{voucherUUID_1}}
  body: json
  auth: none
}

body:json {
  {
      "updater": "{{user_1}}",
      "transactionDate": "2022-07-05"
  }
}

tests {
  test("Update voucher 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
