meta {
  name: attempt post journal
  type: http
  seq: 17
}

post {
  url: {{base_path}}/sob/{{sob}}/journal/{{current_journal_id}}/post
  body: json
  auth: none
}

body:json {
  {
      "poster": "{{user_1}}"
  }
}

tests {
  test("Post journal failed", function () {
      expect(res.getStatus()).to.equal(400);
  });
}
