meta {
  name: dimension migration
  type: http
  seq: 6
}

post {
  url: {{base_path_private}}/internal/dimensions/migrate
  body: none
  auth: none
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
