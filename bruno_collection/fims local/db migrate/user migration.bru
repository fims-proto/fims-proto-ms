meta {
  name: user migration
  type: http
  seq: 5
}

post {
  url: {{base_path_private}}/internal/users/migrate
  body: none
  auth: none
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
