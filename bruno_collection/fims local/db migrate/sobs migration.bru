meta {
  name: sobs migration
  type: http
  seq: 1
}

post {
  url: {{base_path_private}}/internal/sobs/migrate
  body: none
  auth: none
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
