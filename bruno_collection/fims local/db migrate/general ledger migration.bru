meta {
  name: general ledger migration
  type: http
  seq: 3
}

post {
  url: {{base_path_private}}/internal/general-ledgers/migrate
  body: none
  auth: none
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
