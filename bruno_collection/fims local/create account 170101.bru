meta {
  name: create account 170101
  type: http
  seq: 12
}

post {
  url: {{base_path}}/sob/{{sob}}/accounts
  body: json
  auth: inherit
}

body:json {
  {
    "balanceDirection": "debit",
    "class": "1",
    "group": "102",
    "levelNumber": 1,
    "superiorAccountNumber": "1701",
    "title": "品牌使用费"
  }
}

tests {
  test("Create account", function () {
    expect(res.getStatus()).to.equal(201)
    expect(res.body.accountNumber).to.equal('170101')
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
