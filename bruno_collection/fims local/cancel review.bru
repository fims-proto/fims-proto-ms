meta {
  name: cancel review
  type: http
  seq: 15
}

post {
  url: {{base_path}}/sob/{{sob}}/journal/{{current_journal_id}}/cancel-review
  body: json
  auth: none
}

body:json {
  {
      "reviewer": "{{user_2}}"
  }
}

tests {
  test("Cancel review journal", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
