meta {
  name: create account 100201
  type: http
  seq: 10
}

post {
  url: {{base_path}}/sob/{{sob}}/accounts
  body: json
  auth: inherit
}

body:json {
  {
    "balanceDirection": "debit",
    "class": "1",
    "group": "101",
    "levelNumber": 1,
    "superiorAccountNumber": "1002",
    "title": "招商银行"
  }
}

tests {
  test("Create account", function () {
    expect(res.getStatus()).to.equal(201)
    expect(res.body.accountNumber).to.equal('100201')
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
