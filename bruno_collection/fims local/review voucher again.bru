meta {
  name: review journal again
  type: http
  seq: 18
}

post {
  url: {{base_path}}/sob/{{sob}}/journal/{{current_journal_id}}/review
  body: json
  auth: none
}

body:json {
  {
      "reviewer": "{{user_2}}"
  }
}

tests {
  test("Review journal", function () {
      expect(res.getStatus()).to.equal(204);
  });

}
