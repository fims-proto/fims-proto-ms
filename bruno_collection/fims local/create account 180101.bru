meta {
  name: create account 180101
  type: http
  seq: 13
}

post {
  url: {{base_path}}/sob/{{sob}}/accounts
  body: json
  auth: inherit
}

body:json {
  {
    "balanceDirection": "debit",
    "class": "1",
    "group": "102",
    "levelNumber": 1,
    "superiorRawAccountNumber": "001801",
    "title": "房租"
  }
}

tests {
  test("Create account", function () {
    expect(res.getStatus()).to.equal(201)
    expect(res.body.rawAccountNumber).to.equal('001801000001')
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
