meta {
  name: audit voucher again
  type: http
  seq: 19
}

post {
  url: {{base_path}}/sob/{{sob}}/voucher/{{current_voucher_id}}/audit
  body: json
  auth: none
}

body:json {
  {
      "auditor": "{{user_3}}"
  }
}

tests {
  test("Audit voucher", function () {
      expect(res.getStatus()).to.equal(204)
  })
  
}
