meta {
  name: audit journal again
  type: http
  seq: 19
}

post {
  url: {{base_path}}/sob/{{sob}}/journal/{{current_journal_id}}/audit
  body: json
  auth: none
}

body:json {
  {
      "auditor": "{{user_3}}"
  }
}

tests {
  test("Audit journal", function () {
      expect(res.getStatus()).to.equal(204)
  })

}
