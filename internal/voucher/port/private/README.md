# Private Port package
> Internal use of port package.  

Ports under this package (irrespective of http/grpc/others) will not be exposed to public.  
It's for internal call among backend microservices (if we use ms architecture) or modules.